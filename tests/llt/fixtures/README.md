# LLT 测试夹具

本目录存放**冻结的测试夹具**，与 `configs/` 下的工作配置解耦。

- `test.json`：从 `configs/base.json` 复制的快照，供 LLT 用例做加载/解析/仿真冒烟。
  **不要**为了调仿真去改它——`configs/base.json` 才是日常调参用的工作配置。
  改这里只为配合测试断言（例如新增/调整需要被校验的字段）。
